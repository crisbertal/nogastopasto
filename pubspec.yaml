name: no_gasto_pasto
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2 
  provider: ^4.0.1
  intl: ^0.16.1
  moor_flutter: ^2.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  moor_generator: ^2.2.0
  build_runner:

flutter:
  uses-material-design: true

  