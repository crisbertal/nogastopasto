import 'package:flutter/material.dart';

class AddCategoryView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Agregar categoria'),
      ),
      body: Center(child: Text('Agregar categoria')),
    );
  }
}
